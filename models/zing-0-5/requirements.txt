# The Runtime release is pinned by build.runtime_version in reactor.yaml.
# Keep only model dependencies here; do not install reactor-runtime directly.
accelerate==1.13.0
av>=18.0.0
diffusers==0.35.0
huggingface-hub<1.0
ninja
numpy==2.2.6
packaging
pillow==11.3.0
PyYAML==6.0.3
safetensors==0.4.5
sentencepiece==0.2.1
torch==2.9.1
transformers==4.56.0
