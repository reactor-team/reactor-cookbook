# Python dependencies installed into the model image in one pip pass, so a
# conflict between the runtime and DIAMOND's dependencies fails during build.
#
# reactor-runtime is the runtime itself; releases are immutable and there is no
# floating `latest` version.
reactor-runtime==3.1.2

# Use PyTorch's CUDA 12.8 wheels so NVIDIA Blackwell GPUs are supported. This
# index publishes Linux x86_64 and ARM64 wheels; PyPI remains the primary index
# for the rest of the serving environment.
--extra-index-url https://download.pytorch.org/whl/cu128

# DIAMOND imports its inference environment through the public CSGO source
# checkout baked into the image by the Dockerfile. The adapter stubs ale-py and
# wandb because their APIs are unused by CSGO inference; the packages below are
# still needed to import and run the model. NumPy follows Runtime's floor, and
# h5py uses a Python 3.12 Linux ARM64 wheel.
gymnasium==0.29.1
h5py==3.12.1
huggingface-hub==0.17.2
hydra-core==1.3.2
numpy==2.1.3
opencv-python-headless==4.10.0.84
pillow==10.3.0
pygame==2.5.2
torch==2.7.1+cu128
torcheval==0.0.7
tqdm==4.66.4
